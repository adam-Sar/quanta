# Continuing development

## Current milestone

Task 1 (foundation) and Task 2 (dataset ingestion) are complete. Task 2 delivers safe streaming CSV/Parquet ingestion into durable PostgreSQL tables with a typed REST surface, structured error envelope, and full local toolchain. There is no frontend, profiling, detection, AI, recommendation, or validation yet.

## Incremental delivery rule

For each approved task: state goal and architecture, identify files/dependencies, implement only real behavior, add meaningful tests and fixtures, update these documents and OpenAPI contracts, run all prior checks, report limitations, then stop. Planned packages should be created when they gain behavior, not as empty architecture theater.

## Next approved sequence

1. **Task 3 profiling:** dataset and column profiles through Polars lazy scans and PyArrow Parquet metadata. Structured profile schema, profiling service, profiling routes, profile endpoints, profile storage.
2. **Task 4 detection:** standardized finding model, missingness, duplicates, invalid values, robust outliers, categorical inconsistencies, then schema, distribution drift, referential integrity, and relationship discovery.
3. **Task 5 scoring:** explainable severity, two confidence concepts, and a decomposable quality score with documented formula.
4. **Task 6 history:** version/profile/schema/distribution comparison, drift detection, lineage.
5. **Task 7 AI:** provider abstraction and structured reasoning over findings, not datasets.
6. **Task 8 recommendations:** constrained operations, never executable code.
7. **Task 9 validation:** deterministic preview and side-effect checks before execution.
8. **Task 10 API completion:** durable analysis job resource, frontend-ready contracts.
9. **Task 11 hardening:** measured performance, security, limits, worker infrastructure if justified.

## Engineering standards

- Python 3.12 typing and Pydantic v2 at external boundaries.
- Small routes delegate to services; services use repositories/interfaces; processing and provider adapters remain replaceable.
- SQLAlchemy entities are internal. Alembic revisions are reviewed, not blindly autogenerated.
- Original files and dataset versions are immutable; write workflows require cleanup and idempotency planning.
- Statistical methods document assumptions, minimum sample sizes, sampling, algorithm versions, complexity, and failure modes.
- AI consumes bounded structured findings, returns validated structures, and never executes arbitrary code.
- No logs contain files, rows, credentials, tokens, request bodies, or provider prompts with sensitive evidence.
- Tests assert business outcomes and negative cases. Real PostgreSQL tests use disposable infrastructure.

## Definition of done per task

```text
[ ] Architecture and scope recorded
[ ] API/persistence contracts explicit
[ ] Implementation contains no fake success paths
[ ] Unit + API/integration tests cover positive and failure behavior
[ ] Existing pytest, Ruff, mypy, Alembic, and container checks pass
[ ] Documentation states implementation status and limitations
[ ] No later task or frontend work slipped into scope
```

## Migration workflow

After Task 2 model imports are wired to `Base.metadata`:

```bash
alembic revision --autogenerate -m "create dataset ingestion tables"
alembic upgrade head
alembic downgrade -1
alembic upgrade head
```

Review types, indexes, constraints, server defaults, locks, and data migration safety. Production downgrades that would destroy data may deliberately fail with a documented forward-fix strategy.

## Dependency policy

Add dependencies in the task that uses them. Keep versions bounded, update the lock/deployment strategy before production, and avoid overlapping libraries without a documented role. Polars is the default frame engine; DuckDB handles analytical SQL and file/multi-dataset scans; PyArrow is interchange/Parquet; Pandas appears only for compatibility.
