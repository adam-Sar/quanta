"""Profiling domain package.

Task 3 introduces deterministic dataset and column profiles computed from
the original on-disk upload. All numeric metrics are produced by
``profile.metrics`` and persisted by ``ProfilingService``. AI, detection,
recommendation, and validation are not implemented in Task 3.
"""
